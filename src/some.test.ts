import { describe, it, expect, beforeEach, afterEach, vi } from "vitest";

describe("Game", () => {
	beforeEach(() => {});
  
	afterEach(() => {
	  vi.restoreAllMocks();
	});

	it("", () => expect(undefined).toBeUndefined());
});